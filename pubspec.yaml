name: flutter_module
description: A new Flutter module.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  module:
    androidPackage: com.example.fluttermodule
    iosBundleIdentifier: com.example.flutterModule
  assets:
    - static/timg.jpg # 引入本地资源图片, 使用的时候直接将此图片路径放入
